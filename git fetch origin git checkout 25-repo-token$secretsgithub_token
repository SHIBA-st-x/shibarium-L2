git fetch origin
https://github.dev/SHIBA-st-x/shibarium-L2/blob/smart-contract-master/git%20fetch%20origin%20git%20checkout%2025-repo-token%24secretsgithub_token
git checkout 25-repo-token$secretsgithub_token
# This is a basic workflow that is manually triggered
git checkout 25-repo-token$secretsgithub_token_shibastax

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - ryoshi: Send greeting "HelloWorld
      run: echo "Hello ${{ github.event.inputs.shibastax }}"